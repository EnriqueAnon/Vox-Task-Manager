// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  root: 'src', // Change 'src' to the actual folder name if different
  build: {
    outDir: '../dist' // This ensures the output 'dist' folder is still in the project root
  }
});